// integration test: basic arithmetic
fn add(a: i32, b: i32) => i32 { a + b }
fn mul(a: i32, b: i32) => i32 { a * b }

fn main() => i32 {
    let x = add(3, 4)
    let y = mul(x, 2)
    @assert(y == 14, "arithmetic failed")
    0
}
